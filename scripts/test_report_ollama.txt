================================================================================
EVA RESEARCH AI - SCRIPT TEST REPORT (Ollama + Embeddings)
================================================================================
Test order: (1) Ingestion  (2) Retrieval  (3) Inventory  (4) Preview  (5) Check numbers
Generated: (see run date below)

--------------------------------------------------------------------------------
1. CONFIGURATION USED FOR TESTING
--------------------------------------------------------------------------------
Environment (from .env; overrides for local testing):
  EMBED_MODEL             = azure_ada | bge_m3
  DEFAULT_LLM             = azure | ollama_qwen2.5 | ollama_llama3.1
  OLLAMA_BASE_URL         = http://localhost:11434 (host) or http://ollama:11434 (Docker)
  VECTOR_DB_URL / QDRANT_URL = http://localhost:6333 (host) or http://qdrant:6333 (Docker)
  QDRANT_COLLECTION_PREFIX = rag_
  CHUNK_SIZE              = 1200
  CHUNK_OVERLAP           = 150
  LOCAL_LLM_MODEL_PRIMARY = qwen2.5:7b-instruct
  LOCAL_LLM_MODEL_SECONDARY = llama3.1:8b-instruct
  STATE_ROOT              = ./state or /state (Docker)

Prerequisites:
  - Qdrant running (port 6333)
  - Ollama running: ollama pull qwen2.5:7b-instruct; ollama pull llama3.1:8b-instruct
  - Sample data: ./data/<collection_name>/*.pdf or *.docx for ingestion

--------------------------------------------------------------------------------
2. INGESTION (run first)
--------------------------------------------------------------------------------

--- 2.1 index_text.py ---
Purpose: Ingest documents -> extract text -> chunk -> embed -> upsert to Qdrant.
Parameters:
  --data-root   Root directory containing collection folders (e.g. /data or ./data)
  --collection  Single collection name (subfolder under data-root)
  --all-collections  Process all subfolders under data-root (mutually exclusive with --collection)

Example commands:
  python scripts/index_text.py --data-root ./data --collection hydrogen_books
  python scripts/index_text.py --data-root ./data --all-collections

Configuration used: EMBED_MODEL (azure_ada or bge_m3) sets embedding; vector size matches model (1536 or 1024).
Output: Chunks upserted to Qdrant; ingestion logs under STATE_ROOT (for inventory).

RESULTS:
(append below when test is run)


--------------------------------------------------------------------------------
3. RETRIEVAL (run after ingestion)
--------------------------------------------------------------------------------

--- 3.1 query_chunks.py ---
Purpose: Retrieve top-k chunks for inspection (no LLM answer).
Parameters:
  --q           Query text
  --collection  Collection name (use same as in ingestion; prefix applied if QDRANT_COLLECTION_PREFIX set)
  --topk        Number of chunks (default 8)
  --embedding   Embedding model id (default from EMBED_MODEL)

Example commands:
  python scripts/query_chunks.py --q "electrolyzer efficiency" --collection hydrogen_books --topk 8
  python scripts/query_chunks.py --q "hydrogen storage" --collection hydrogen_books --topk 5

Output: For each hit: score, chunk_id, source_path, page_start, page_end, text_preview.

RESULTS:
(append below when test is run)


--- 3.2 answer_local.py ---
Purpose: Retrieve chunks -> call Ollama -> return answer with citations.
Parameters:
  --q           Question
  --collection  Collection name
  --model       Ollama model (e.g. qwen2.5:7b-instruct, llama3.1:8b-instruct)
  --topk        Chunks to retrieve (default 5)
  --embedding   Embedding model id (default from EMBED_MODEL)

Example commands:
  python scripts/answer_local.py --q "What is hydrogen bunkering?" --collection hydrogen_books --model qwen2.5:7b-instruct
  python scripts/answer_local.py --q "Explain electrolyzer efficiency" --collection hydrogen_books --model llama3.1:8b-instruct

Prompt rule: Use only the provided context. If not found, say NOT FOUND. Cite as (source_path p.page_start–page_end).

RESULTS:
(append below when test is run)


--------------------------------------------------------------------------------
4. INVENTORY (run after ingestion)
--------------------------------------------------------------------------------

--- 4.1 report_inventory.py ---
Purpose: Produce ingestion report (docs/chunks/skips) from ingestion logs.
Parameters:
  --state   State directory containing ingestion_*.json logs (e.g. /state or ./state)
  --verbose Print detailed JSON to console

Example commands:
  python scripts/report_inventory.py --state ./state
  python scripts/report_inventory.py --state /state --verbose

Output file: <state>/reports/inventory.json (and timestamped copy).

RESULTS:
(append below when test is run)


--------------------------------------------------------------------------------
5. PREVIEW (inspect extracted text; can run before or after ingestion)
--------------------------------------------------------------------------------

--- 5.1 preview_extract.py ---
Purpose: Show extracted text for inspection (page-aware for PDF).
Parameters:
  --file   Path to document (PDF or DOCX)
  --pages  Page range for PDF (e.g. 1-2, 1,3,5)
  --head   Number of characters for DOCX
  --all    Show all pages (PDF) or full text (DOCX)

Example commands:
  python scripts/preview_extract.py --file "./data/hydrogen_books/book1.pdf" --pages 1-2
  python scripts/preview_extract.py --file "./data/biofuels_books/doc.docx" --head 2000
  python scripts/preview_extract.py --file "./data/hydrogen_books/book1.pdf" --all

